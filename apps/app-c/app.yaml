apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: app-c
spec:
  components:
  - name: app-c
    properties:
      env:
      - name: username
        valueFrom:
          secretKeyRef:
            key: username
            name: app-c-db
      - name: password
        valueFrom:
          secretKeyRef:
            key: password
            name: app-c-db
      - name: endpoint
        valueFrom:
          secretKeyRef:
            key: endpoint
            name: app-c-db
      - name: port
        valueFrom:
          secretKeyRef:
            key: port
            name: app-c-db
      image: sselzer/app-c:3f4c703
      port: 8080
    traits:
    - properties:
        domain: app-c.127.0.0.1.nip.io
        http:
          /: 8080
      type: gateway
    - properties:
        cpuPercent: 80
        max: 5
        min: 2
      type: cpuscaler
    type: webservice
