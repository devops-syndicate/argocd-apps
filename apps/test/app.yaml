apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: test
spec:
  components:
  - name: test
    properties:
      env:
      - name: username
        valueFrom:
          secretKeyRef:
            key: username
            name: test-db
      - name: password
        valueFrom:
          secretKeyRef:
            key: password
            name: test-db
      - name: endpoint
        valueFrom:
          secretKeyRef:
            key: endpoint
            name: test-db
      - name: port
        valueFrom:
          secretKeyRef:
            key: port
            name: test-db
      image: sselzer/test:afdc066
      port: 8080
    traits:
    - properties:
        domain: test.127.0.0.1.nip.io
        http:
          /: 8080
      type: gateway
    - properties:
        cpuPercent: 80
        max: 5
        min: 2
      type: cpuscaler
    - type: argocd
    type: webservice
